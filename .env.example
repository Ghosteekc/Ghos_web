# Базовый URL backend API.
# Пустое значение — запросы идут на тот же origin (/api/...).
# В dev Vite проксирует /api на localhost:8080 (см. vite.config.ts).
#
# Production (frontend на Vercel, backend отдельно):
# VITE_API_URL=https://your-backend.example.com
VITE_API_URL=
